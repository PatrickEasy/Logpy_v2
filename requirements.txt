# Logpy_v2 Requirements
#
# Runtime dependencies are only needed for the `convert` module. The
# logging (printtime), timing (timer), coloured-output (print_utils),
# and scaffolding (scaffold) modules are pure standard library.
#
# Python: 3.9+

# For convert.py (md -> docx, csv <-> json):
markdown>=3.4
python-docx>=1.0
beautifulsoup4>=4.12

# Optional Development Tools (uncomment if needed):
# pytest>=7.4          # For unit testing
# pytest-cov>=4.1      # For test coverage reports
# black>=23.0          # For code formatting
# flake8>=6.0          # For linting
# mypy>=1.0            # For type checking
